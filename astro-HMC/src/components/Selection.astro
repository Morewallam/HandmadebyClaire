---
import type { SanityImageSource } from "@sanity/image-url";
import ArtProduct, { type HalfProduct } from "./ArtProduct.astro";
import TitleImage from "./TitleImage.astro";

export interface Page {
  image: SanityImageSource;
  title: string;
  products: HalfProduct[];
}

export interface Props {
  page: Page;
}

const {
  page: { image, title, products },
} = Astro.props;
---

<div>
  <TitleImage image={image} title={title} />

  <div
    class="pt-8 pb-8 p-2 md:p-8 flex flex-wrap gap-3 justify-center items-center"
  >
    {
      products.length > 0 ? (
        products.map((item) => (
          <div>
            <ArtProduct
              mainImage={item.mainImage}
              name={item.name}
              slug={item.slug}
              theme={item.theme}
              category={item.category}
            />
          </div>
        ))
      ) : (
        <div class="center">
          <span class="text-center">
            There are currently no {title} products
          </span>
        </div>
      )
    }
  </div>
</div>
