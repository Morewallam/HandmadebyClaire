---
import type { SanityImageSource } from "@sanity/image-url";
import ImageRender from "./ImageRender.astro";

export interface Props {
  image: SanityImageSource;
  name: string;
  slug: { current: string };
  type: "theme" | "category" | "occasion";
  size?: "small" | "large";
}

const { image, name, slug, type = "theme", size = "small" } = Astro.props;
---

{
  size === "small" && (
    <a href={`/works/${type}/${slug.current}`} class="block">
      <div class="w-30 grid place-items-center shadow-md/20 bg-white p-3 group transition-transform duration-300 hover:scale-110">
        <div class="w-20 h-20 drop-shadow-md/30">
          <ImageRender
            image={image}
            height={100}
            alt={name}
            style="w-full h-full object-contain rounded-md"
          />
        </div>
        <div class="flex items-center text-center h-10">
          <span class="align-middle">{name}</span>
        </div>
      </div>
    </a>
  )
}

{
  size === "large" && (
    <a href={`/works/${type}/${slug.current}`} class="block">
      <div class="p-4 shadow-xs bg-stone-100 group transition-all duration-300 hover:scale-110 max-w-38 md:max-w-60  hover:shadow-lg ">
        <ImageRender
          image={image}
          alt={name}
          width={500}
          style="aspect-rectangle drop-shadow object-cover h-30 w-30 md:h-40 md:w-40 object-center"
        />
        <div class="mt-4 h-10">
          <div>
            <h3 class="text-sm md:text-base text-gray-700 text-wrap ">
              <span aria-hidden="true" class="inset-0" />
              {name}
            </h3>
          </div>
        </div>
      </div>
    </a>
  )
}
